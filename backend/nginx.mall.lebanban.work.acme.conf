server {
    listen 80;
    listen [::]:80;
    server_name mall.lebanban.work;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 404;
    }
}
